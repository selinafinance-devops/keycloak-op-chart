apiVersion: v1
kind: Service
metadata:
  name: {{ include "keycloak-op-chart.fullname" . }}
  labels:
    {{- include "keycloak-op-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keycloak-op-chart.selectorLabels" . | nindent 4 }}
